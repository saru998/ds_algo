#converting number to list of integer
    num = list(map(int,input()))

#converting list of integer to single int,number
    num = int("".join(map(str, num_list))) 

#converting string to list
    exp = list(input())

